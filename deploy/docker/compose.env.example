# Copy to .env.docker, generate a long random token, then start with:
# docker compose --env-file .env.docker up --build -d
MFCV_API_TOKEN=replace-with-a-long-random-token

# Optional local host port. Docker remains bound to 127.0.0.1.
MFCV_PORT=3000
