# Vitest / live integration tests only (gitignored as .env.test).
# Copy your key once, e.g. from lifestyle:
#   grep '^OPENROUTER_API_KEY=' /mnt/c/git/lifestyle/.env > .env.test
OPENROUTER_API_KEY=your_openrouter_api_key_here